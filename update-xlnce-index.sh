#!/bin/bash
# Скрипт для обновления индекса git: файлы из Лаборатория ДКЦП/xlnce/ добавляются в корень репозитория

REPO_ROOT="$(git rev-parse --show-toplevel)"
XLNCE_DIR="$REPO_ROOT/xlnce"

if [ ! -d "$XLNCE_DIR" ]; then
    echo "❌ Папка xlnce не найдена: $XLNCE_DIR"
    exit 1
fi

echo "🔄 Обновление индекса git для файлов из xlnce..."
echo ""

# Теперь xlnce в корне, поэтому просто добавляем файлы как есть
# Никаких преобразований путей не требуется
echo "ℹ️  Папка xlnce теперь в корне репозитория, преобразование путей не требуется"

echo ""
echo "✨ Готово! Индекс обновлён."

